# Plan: 14.7-04 — Discord Page Fix

**Status:** COMPLETE

## Changes Made

### `src/ZedsuFrontend/src/scripts/pages/discord.js`

1. **Safe indexOf** — All `.indexOf()` calls on `discord.events` and `kill_milestones` use `asArray()` from normalizers.js. Never crashes on non-array.

2. **Normalize on load** — `normalizeDiscordConfig()` called once on page load, result stored in `_currentConfig`. All rendering and comparisons use the normalized values.

3. **No page reload on save** — Event toggles and milestone chips update via individual API calls. No `load()` re-render after save. UI updates in-place.

4. **Rollback on failure** — If save fails, event checkboxes and milestone chips revert to `_currentConfig` state.

5. **Test button disabled** — When `has_webhook === false`, test button is `disabled` and `testWebhook()` returns early.

6. **Loading state on save/clear/test** — Buttons disabled + text changes to "Saving..." / "Clearing..." / "Sending..." while in-flight.

7. **Webhook URL never rendered** — `type=password` input, masked with `********************************` on save, never shows real URL.

8. **Cleanup function** — `cleanupDiscordPage()` exported and registered with page lifecycle.

## Verification

- Load page with malformed config (discord.events = null / object / string) — no crash
- Toggle event with no webhook configured — disabled and returns early
- Save event — no page flicker
- Save fails — UI rolls back to previous state
- Navigate away — no stale listeners
