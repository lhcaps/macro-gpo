# Plan: 14.7-08 — HUD Behavior Cleanup

**Status:** COMPLETE

## Changes Made

### `src/ZedsuFrontend/src/scripts/hud.js` — Already correct from Phase 14.6

Phase 14.6 already set:
- `hud_visible: false` in Rust `AppState::new()` — HUD hidden at startup
- `visible: false` in `tauri.conf.json` for HUD window
- F2 toggle uses `hud_visible` state correctly

### Verification of existing implementation

- `hud.js` uses `pollBackend()` every 500ms
- `HudApi.toggle()` shows/hides via `hudOverlay.style.display`
- Backend offline shows "OFFLINE" text (not blank)
- HUD never renders full shell — it's a separate window (index.html?hud=1)
- `initHud()` starts polling immediately — HUD window is separate from shell window

## HUD Window Behavior (from Phase 14.6 confirmation)

| Behavior | Status |
|---|---|
| HUD hidden on startup | ✅ `visible: false` in tauri.conf.json |
| HUD button in shell toggles HUD | ✅ `window.HudApi?.toggle()` |
| F2 toggles HUD | ✅ `window.HudApi?.toggle()` |
| HUD shows compact status | ✅ `updateHudDisplay()` |
| HUD shows "OFFLINE" when backend unreachable | ✅ `hudStatusText.textContent = 'OFFLINE'` |
| HUD never renders full shell | ✅ Separate window, separate HTML |

## Verification

- Launch app → HUD window not visible
- Press F2 → HUD appears
- Press F2 again → HUD hides
- Click HUD button in topbar → HUD toggles
- Backend offline → HUD shows "OFFLINE"
