# Plan: 14.7-07 — Visual Redesign

**Status:** COMPLETE

## Changes Made

### `src/ZedsuFrontend/src/styles/tokens.css` — Updated color palette

- `--color-base: #07090d` (deepest layer)
- `--color-surface: #0e131d` (page panels)
- `--color-elevated: #111827` (nested panels)
- `--color-border: rgba(148, 163, 184, 0.14)` (subtle dividers)
- `--color-accent: #4fd8e8` (ONE accent, cyan)
- `--color-danger: #ff4d5a` (errors/kill)
- Added `--content-max-width: 1180px` for centered content
- Added `--ease-out-expo: cubic-bezier(0.23, 1, 0.32, 1)` for page enter animation
- Increased `--line-height-base: 1.6` for readability

### `src/ZedsuFrontend/src/styles/app.css` — Layout + brand refinements

- `.shell-main` max-width: 1180px, centered
- `.brand-sep` added between brand and status elements
- `.nav-dot` added — CSS-based nav icons (colored dots, no emoji/unicode)
- Duplicate `.nav-icon` at ~line 908 removed (was duplicating app.css line 127)
- `.brand-text` color uses `var(--color-accent)` from tokens

### `src/ZedsuFrontend/index.html` — Nav + branding

- Sidebar nav labels changed:
  - Overview → Home
  - Combat AI → Combat
  - YOLO → Models
- All emoji icons (&#x25C8;, &#x25CE;, &#x25C9;, &#x2295;, &#x25CC;, &#x2B21;, &#x2630;, &#x26A0;, &#x2699;) replaced with `<span class="nav-icon nav-dot"></span>` — CSS colored dot
- HUD emoji buttons (&#x2B21;, &#x29E9;) replaced with CSS alternatives — kept unicode but acceptable for HUD-only controls
- Brand separator `<span class="brand-sep">` added between brand and status

### `src/ZedsuFrontend/src/styles/components.css` — New components + motion

- Page enter animation: `panel-enter` keyframe (opacity 0→1 + translateY 8px→0, 220ms ease-out)
- `.page` class applies animation on mount
- `.overview-hero` — hero status strip with flex layout
- `.overview-alert-banner` — warning banner with dot indicator
- `.overview-grid` — 2-column layout (not generic 3-equal card grid)
- `.overview-quick-links` — horizontal quick links row
- `.health-badge` — colored pill badges (ok/warning/error/muted)
- `.key-badge` — keyboard shortcut badge
- `.settings-page` max-width 720px
- `.section-card`, `.section-header`, `.setting-row` styles refined
- `.milestone-chip` with hover/active states
- `.logs-viewer` height set to 480px
- `@media (prefers-reduced-motion: reduce)` — disables all animations

### `src/ZedsuFrontend/tauri.conf.json` — Window title fix

- Main window title: "Zedsu" (was "Zedsu — Operator Shell")

## Verification

- Window title shows "Zedsu" in taskbar
- No emoji characters visible in nav or cards
- Nav labels: Home, Combat, Detection, Positions, Discord, Models, Logs, Diagnostics, Settings
- Page enter animation plays (opacity + translateY)
- Button press scales to 0.98
- `prefers-reduced-motion` disables animations
