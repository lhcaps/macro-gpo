# 12-5-04-PLAN.md — Scored Movement Policy

**Plan number:** 12-5-04
**Wave:** 2 — Survival Intelligence
**Phase:** 12.5 — Combat AI Intelligence Foundation
**Goal:** Replace pure random pattern selection in `perform_dynamic_combat_movement()` with scored action selection based on situation.
**Status:** Ready

---

## What to Do

### Step 1: Create `src/core/movement_policy.py`

File: `src/core/movement_policy.py`

```python
"""
Movement Policy — Phase 12.5.

Scores and selects combat movement actions based on situation context.
Replaces pure-random pattern selection in `perform_dynamic_combat_movement()`.

Design decisions:
- Scoring is additive: each factor adds or subtracts from base score
- Repeated action penalty prevents stuttering on the same action
- Random fallback ensures bot always moves even with incomplete context
- Intent from situation model maps to movement category
- Existing `perform_dynamic_combat_movement()` is preserved as backward-compatible wrapper
"""
from __future__ import annotations

import random
import time
from dataclasses import dataclass
from typing import Literal, Optional

# ---------------------------------------------------------------------------
# Types
# ---------------------------------------------------------------------------

MovementAction = Literal[
    "strafe_left",
    "strafe_right",
    "forward_left",
    "forward_right",
    "backward_left",
    "backward_right",
    "short_backstep",
    "camera_correct_left",
    "camera_correct_right",
    "scan_left",
    "scan_right",
    "hold_position",
]

# ---------------------------------------------------------------------------
# Action Definition
# ---------------------------------------------------------------------------

@dataclass
class ScoredAction:
    """An action candidate with its computed score."""
    name: MovementAction
    score: float
    reason: str
    keys: list[str]
    duration_range: tuple[float, float]


# ---------------------------------------------------------------------------
# MovementPolicy
# ---------------------------------------------------------------------------

class MovementPolicy:
    """
    Scores and selects movement actions based on combat situation.

    Usage:
        policy = MovementPolicy(config)
        action = policy.choose_action(
            intent="reposition",           # From CombatSituationModel
            situation=situation,           # CombatSituation
            target_decision=target_dec,    # TargetDecision
            last_action="strafe_left",     # Previously selected action
        )
        # action.name, action.keys, action.duration_range
    """

    def __init__(self, config: Optional[dict] = None):
        cfg = (config or {}).get("combat_ai", {})
        self._enabled = cfg.get("movement_policy") == "scored"
        self._fallback_random = cfg.get("random_movement_fallback", True)
        self._repeat_penalty = cfg.get("repeated_action_penalty", 0.15)
        self._reposition_threshold = cfg.get("reposition_crowd_threshold", 0.70)
        self._flee_hp_threshold = cfg.get("flee_crowd_hp_threshold", 0.55)

        # Track last action to apply repeat penalty
        self._last_action: Optional[MovementAction] = None
        self._last_action_ts: float = 0.0

    def reset(self) -> None:
        """Reset policy state for new match."""
        self._last_action = None
        self._last_action_ts = 0.0

    def choose_action(
        self,
        intent: str,              # "engage" | "pursue" | "reposition" | "flee" | "scan"
        situation,               # CombatSituation dataclass
        target_decision=None,     # TargetDecision dataclass
        last_action: Optional[str] = None,
    ) -> ScoredAction:
        """
        Choose the best movement action for the current situation.

        Args:
            intent: recommended intent from situation model
            situation: CombatSituation with crowd_risk, player_hp_low, etc.
            target_decision: TargetDecision with center_error_x/y
            last_action: name of the previously selected action

        Returns:
            ScoredAction with name, score, reason, keys, duration_range
        """
        if not self._enabled:
            return self._random_fallback()

        # Gather scoring context
        crowd_risk = getattr(situation, "crowd_risk", 0.0) if situation else 0.0
        player_hp_low = getattr(situation, "player_hp_low", False) if situation else False
        death_risk = getattr(situation, "death_risk", 0.0) if situation else 0.0
        target_visible = getattr(target_decision, "target_visible", False) if target_decision else False

        err_x = getattr(target_decision, "center_error_x", 0.0) if target_decision else 0.0
        err_y = getattr(target_decision, "center_error_y", 0.0) if target_decision else 0.0

        # Generate candidate actions based on intent
        candidates = self._get_candidates_for_intent(intent, err_x, err_y)

        # Score each candidate
        scored = []
        for candidate in candidates:
            score = self._score_action(
                action=candidate,
                intent=intent,
                crowd_risk=crowd_risk,
                player_hp_low=player_hp_low,
                target_visible=target_visible,
                err_x=err_x,
                err_y=err_y,
                last_action=last_action or self._last_action,
            )
            scored.append((score, candidate))

        # Sort by score descending
        scored.sort(key=lambda x: x[0].score, reverse=True)
        best = scored[0][0]

        # Record for repeat penalty
        self._last_action = best.name
        self._last_action_ts = time.time()

        # If best score is negative and fallback is enabled, use random
        if best.score < -0.3 and self._fallback_random:
            return self._random_fallback()

        return best

    def _get_candidates_for_intent(
        self,
        intent: str,
        err_x: float,
        err_y: float,
    ) -> list[ScoredAction]:
        """Get available movement actions for a given intent."""
        # Base candidates available in all intents
        base = [
            ScoredAction("strafe_left", 0.0, "base", ["a"], (0.11, 0.20)),
            ScoredAction("strafe_right", 0.0, "base", ["d"], (0.11, 0.20)),
            ScoredAction("short_backstep", 0.0, "base", ["s"], (0.10, 0.18)),
            ScoredAction("hold_position", 0.0, "base", [], (0.05, 0.12)),
        ]

        if intent == "engage":
            return base + [
                ScoredAction("forward_left", 0.0, "engage", ["w", "a"], (0.14, 0.24)),
                ScoredAction("forward_right", 0.0, "engage", ["w", "d"], (0.14, 0.24)),
                ScoredAction("camera_correct_left", 0.0, "engage", [], (0.08, 0.15)),
                ScoredAction("camera_correct_right", 0.0, "engage", [], (0.08, 0.15)),
            ]

        elif intent == "pursue":
            # Move toward target (camera correction + forward)
            toward = []
            if err_x < -30:
                toward.append(ScoredAction("camera_correct_right", 0.0, "pursue", [], (0.08, 0.15)))
            elif err_x > 30:
                toward.append(ScoredAction("camera_correct_left", 0.0, "pursue", [], (0.08, 0.15)))
            return base + toward + [
                ScoredAction("forward_left", 0.0, "pursue", ["w", "a"], (0.14, 0.24)),
                ScoredAction("forward_right", 0.0, "pursue", ["w", "d"], (0.14, 0.24)),
            ]

        elif intent == "reposition":
            return [
                ScoredAction("backward_left", 0.0, "reposition", ["s", "a"], (0.12, 0.20)),
                ScoredAction("backward_right", 0.0, "reposition", ["s", "d"], (0.12, 0.20)),
                ScoredAction("short_backstep", 0.0, "reposition", ["s"], (0.15, 0.25)),
                ScoredAction("strafe_left", 0.0, "reposition", ["a"], (0.11, 0.20)),
                ScoredAction("strafe_right", 0.0, "reposition", ["d"], (0.11, 0.20)),
                ScoredAction("hold_position", 0.0, "reposition", [], (0.05, 0.12)),
            ]

        elif intent == "flee":
            return [
                ScoredAction("backward_left", 0.0, "flee", ["s", "a"], (0.20, 0.35)),
                ScoredAction("backward_right", 0.0, "flee", ["s", "d"], (0.20, 0.35)),
                ScoredAction("short_backstep", 0.0, "flee", ["s"], (0.20, 0.35)),
                ScoredAction("strafe_left", 0.0, "flee", ["a"], (0.15, 0.25)),
                ScoredAction("strafe_right", 0.0, "flee", ["d"], (0.15, 0.25)),
            ]

        elif intent == "scan":
            return [
                ScoredAction("scan_left", 0.0, "scan", [], (0.15, 0.25)),
                ScoredAction("scan_right", 0.0, "scan", [], (0.15, 0.25)),
                ScoredAction("strafe_left", 0.0, "scan", ["a"], (0.10, 0.18)),
                ScoredAction("strafe_right", 0.0, "scan", ["d"], (0.10, 0.18)),
                ScoredAction("hold_position", 0.0, "scan", [], (0.05, 0.12)),
            ]

        # Fallback for unknown intent
        return base

    def _score_action(
        self,
        action: ScoredAction,
        intent: str,
        crowd_risk: float,
        player_hp_low: bool,
        target_visible: bool,
        err_x: float,
        err_y: float,
        last_action: Optional[str],
    ) -> ScoredAction:
        """
        Score a single action candidate.

        Score = sum of factors:
        - target_visible_score: +0.3 if target visible
        - target_centering_score: +0.2 if camera correction helps center target
        - hit_confirm_score: +0.1 if recent hit confirm
        - spacing_score: +0.2 if reposition/flee and crowd risk high
        - escape_vector_score: +0.2 if backward in flee/reposition
        - crowd_risk_penalty: -0.3 if forward engage in high crowd risk
        - repeated_action_penalty: -0.15 if same as last action
        - target_switch_penalty: -0.2 if moving away from target when visible
        """
        score = 0.0
        reasons = []

        # Target visibility bonus
        if target_visible:
            score += 0.30
            reasons.append("target_visible")

        # Camera correction bonus (only if targeting error is significant)
        if "camera_correct" in action.name:
            if abs(err_x) > 50 or abs(err_y) > 50:
                score += 0.20
                reasons.append("centering_needed")
            else:
                score -= 0.10  # Slight penalty if already centered
                reasons.append("already_centered")

        # Forward movement: good for engage/pursue, bad for reposition/flee
        if action.name in ("forward_left", "forward_right"):
            if intent in ("engage", "pursue"):
                score += 0.20
                reasons.append("forward_engage")
            elif crowd_risk > 0.5:
                score -= 0.30
                reasons.append("forward_crowd_penalty")
            else:
                score -= 0.10

        # Backward movement: good for reposition/flee, bad for engage
        if action.name in ("backward_left", "backward_right", "short_backstep"):
            if intent in ("reposition", "flee"):
                score += 0.25
                reasons.append("backward_escape")
            elif crowd_risk > 0.4:
                score += 0.10
                reasons.append("backward_crowd_caution")
            else:
                score -= 0.05

        # Strafe: neutral in engage, good for reposition
        if action.name in ("strafe_left", "strafe_right"):
            if intent == "reposition":
                score += 0.15
                reasons.append("strafe_reposition")
            elif intent == "engage":
                score += 0.10
                reasons.append("strafe_engage")
            else:
                score += 0.05

        # Scan movement: only good when intent is scan
        if action.name in ("scan_left", "scan_right"):
            if intent == "scan":
                score += 0.30
                reasons.append("scan_intent")
            elif intent == "pursue":
                score += 0.10
                reasons.append("scan_pursue")
            else:
                score -= 0.20

        # Hold position: penalty unless repositioning
        if action.name == "hold_position":
            if intent in ("reposition", "flee"):
                score += 0.05  # Brief hold before next move
            else:
                score -= 0.05

        # Crowd risk penalty for forward engagement in dangerous crowd
        if crowd_risk >= self._reposition_threshold:
            if action.name in ("forward_left", "forward_right"):
                score -= 0.30
                reasons.append("crowd_forward_penalty")
            elif action.name in ("backward_left", "backward_right", "short_backstep"):
                score += 0.15
                reasons.append("crowd_backward_bonus")

        # Low HP: prefer backward/strafe over forward
        if player_hp_low:
            if action.name in ("forward_left", "forward_right"):
                score -= 0.25
                reasons.append("lowhp_forward_penalty")
            elif action.name in ("backward_left", "backward_right", "short_backstep"):
                score += 0.20
                reasons.append("lowhp_backward_bonus")
            elif action.name in ("strafe_left", "strafe_right"):
                score += 0.10

        # Repeated action penalty
        if action.name == last_action:
            score -= self._repeat_penalty
            reasons.append("repeat_penalty")

        return ScoredAction(
            name=action.name,
            score=score,
            reason="|".join(reasons) if reasons else "neutral",
            keys=action.keys,
            duration_range=action.duration_range,
        )

    def _random_fallback(self) -> ScoredAction:
        """Pure random fallback when policy is disabled or score is negative."""
        patterns = [
            ScoredAction("strafe_left", 0.0, "random", ["a"], (0.11, 0.20)),
            ScoredAction("strafe_right", 0.0, "random", ["d"], (0.11, 0.20)),
            ScoredAction("forward_left", 0.0, "random", ["w", "a"], (0.14, 0.24)),
            ScoredAction("forward_right", 0.0, "random", ["w", "d"], (0.14, 0.24)),
            ScoredAction("backward_left", 0.0, "random", ["s", "a"], (0.10, 0.18)),
            ScoredAction("backward_right", 0.0, "random", ["s", "d"], (0.10, 0.18)),
            ScoredAction("short_backstep", 0.0, "random", ["s"], (0.16, 0.28)),
        ]
        return random.choice(patterns)
```

### Step 2: Wire movement policy into `bot_engine.py`

#### 2a. Import at top of `bot_engine.py`

```python
# Phase 12.5: Movement policy
from src.core.movement_policy import MovementPolicy, ScoredAction, MovementAction
```

#### 2b. In `BotEngine.__init__()`, create movement policy

```python
# Phase 12.5: Movement policy
self._movement_policy = MovementPolicy(getattr(app, 'config', {}))
```

#### 2c. In `CombatStateMachine.__init__()`, reference movement policy

```python
# Phase 12.5: Movement policy reference
self._movement_policy: Optional[MovementPolicy] = None
```

Set `self._movement_policy = engine._movement_policy` after `self.engine = engine`.

#### 2d. In `BotEngine.mark_match_active()`, reset movement policy

```python
# Phase 12.5: Reset movement policy on new match
try:
    self._movement_policy.reset()
except Exception:
    pass
```

#### 2e. Replace `perform_dynamic_combat_movement()` body

Keep the function signature unchanged for backward compatibility. Replace the body:

```python
    def perform_dynamic_combat_movement(self, move_keys, bursts=None, situation=None, target_decision=None):
        """
        Phase 12.5: Replaced body with scored movement policy.
        Old random pattern selection is preserved as fallback.

        Args:
            move_keys: [forward, left, backward, right] — kept for fallback
            bursts: number of movement steps (default random 1-3)
            situation: CombatSituation from situation model
            target_decision: TargetDecision from target memory
        """
        steps = bursts if bursts is not None else random.randint(1, 3)
        last_action = None

        for _ in range(steps):
            if not self.is_running():
                return None

            # Phase 12.5: Scored movement selection
            try:
                if self._movement_policy is not None and situation is not None:
                    intent = situation.get("recommended_intent", "engage") if isinstance(situation, dict) else getattr(situation, "recommended_intent", "engage")
                    # Reconstruct situation-like object for policy
                    class _SitStub:
                        def __init__(self, d):
                            self.crowd_risk = d.get("crowd_risk", 0.0)
                            self.player_hp_low = d.get("player_hp_low", False)
                            self.death_risk = d.get("death_risk", 0.0)
                            self.visible_enemy_count = d.get("visible_enemy_count", 0)
                            self.nearby_enemy_count = d.get("nearby_enemy_count", 0)
                    sit = _SitStub(situation) if isinstance(situation, dict) else situation

                    action = self._movement_policy.choose_action(
                        intent=intent,
                        situation=sit,
                        target_decision=target_decision,
                        last_action=last_action,
                    )

                    # Execute the scored action
                    if action.name == "hold_position":
                        # Just sleep briefly
                        if not self.sleep(random.uniform(*action.duration_range)):
                            return False
                        last_action = "hold_position"
                        continue

                    # Map scored action keys to actual key bindings
                    actual_keys = []
                    for k in action.keys:
                        if k == "w":
                            actual_keys.append(move_keys[0])
                        elif k == "a":
                            actual_keys.append(move_keys[1])
                        elif k == "s":
                            actual_keys.append(move_keys[2])
                        elif k == "d":
                            actual_keys.append(move_keys[3])

                    if actual_keys:
                        if not self.hold_movement_keys(actual_keys, random.uniform(*action.duration_range)):
                            return False

                    # Camera correction (moveRel only, no key)
                    # Extract err_x from target_decision for camera movement
                    err_x = getattr(target_decision, "center_error_x", 0.0) if target_decision else 0.0
                    if "camera_correct" in action.name:
                        if "left" in action.name:
                            pydirectinput.moveRel(-abs(int(err_x * 0.5)) if err_x < 0 else -50, 0)
                        elif "right" in action.name:
                            pydirectinput.moveRel(abs(int(err_x * 0.5)) if err_x > 0 else 50, 0)

                    # Scan movement (moveRel controlled, not random 165px)
                    if "scan" in action.name:
                        if "left" in action.name:
                            pydirectinput.moveRel(-60, random.randint(-10, 10))
                        elif "right" in action.name:
                            pydirectinput.moveRel(60, random.randint(-10, 10))

                    last_action = action.name

                else:
                    # Fallback to old random behavior
                    if not self._execute_random_movement_fallback(move_keys):
                        return False
                    last_action = None
            except Exception:
                # Fallback to old random behavior on any error
                if not self._execute_random_movement_fallback(move_keys):
                    return False
                last_action = None

            # Check combat exit conditions
            status = self.handle_combat_exit_conditions(search_context=self.build_search_context())
            if status:
                return status

            if not self.sleep(random.uniform(0.05, 0.12)):
                return False

        return None

    def _execute_random_movement_fallback(self, move_keys) -> bool:
        """Fallback to old pure-random movement behavior."""
        if len(move_keys) < 4:
            return True
        forward, left, backward, right = move_keys[:4]
        patterns = [
            ([left], (0.11, 0.2)),
            ([right], (0.11, 0.2)),
            ([forward, left], (0.14, 0.24)),
            ([forward, right], (0.14, 0.24)),
            ([backward, left], (0.1, 0.18)),
            ([backward, right], (0.1, 0.18)),
            ([forward], (0.16, 0.28)),
        ]
        pattern_keys, duration_range = random.choice(patterns)
        return bool(self.hold_movement_keys(pattern_keys, random.uniform(*duration_range)))
```

#### 2f. Update `_execute_engaged_combat()` to pass situation to movement

In `_execute_engaged_combat()`, update the `perform_dynamic_combat_movement` call:

```python
        self.perform_dynamic_combat_movement(
            [keys_cfg.get("forward", "w"),
             keys_cfg.get("left", "a"),
             keys_cfg.get("backward", "s"),
             keys_cfg.get("right", "d")],
            bursts=random.randint(1, 2),
            situation=action.get("situation"),   # Phase 12.5: pass situation
            target_decision=action.get("target_memory"),  # Phase 12.5: pass target memory
        )
```

#### 2g. Update telemetry to record movement decision

In the tick recording section:

```python
                decision_info = action.get("decision", {})
                movement_info = {
                    "intent": situation_info.get("recommended_intent", "scan") if situation_info else "scan",
                    "last_action": last_action,
                }
                tick = CombatTick(
                    # ... existing fields ...
                    decision={
                        "intent": situation_info.get("recommended_intent", "scan") if situation_info else "scan",
                        "target_memory": target_info,
                        "movement": movement_info,
                    },
                )
```

### Step 3: Export from `src/core/__init__.py`

```python
from .movement_policy import MovementPolicy, ScoredAction, MovementAction
```

---

## Exit Criteria

- `python -m py_compile src/core/movement_policy.py` passes
- `python -m py_compile src/core/bot_engine.py` passes
- `perform_dynamic_combat_movement()` signature unchanged (backward compatible)
- In `reposition` intent: backward/strafe actions score higher than forward
- In `flee` intent: backward actions score highest
- Repeated same action gets -0.15 score penalty (configurable)
- Random fallback used when `movement_policy` config != "scored"
- Random fallback used when all action scores are negative
- Camera correction movement uses controlled pixels (30-80px), not random 165px
- Movement selection logged via telemetry (selected_action in decision dict)
- Old random behavior still accessible as `_execute_random_movement_fallback()`
