# 12-5-00-PLAN.md — Phase 12 Regression Guard

**Plan number:** 12-5-00
**Wave:** 0 — Guardrail
**Phase:** 12.5 — Combat AI Intelligence Foundation
**Goal:** Before touching AI, verify Phase 12.1–12.4 smoke tests pass on a clean codebase.
**Status:** Ready

---

## Problem

Phase 12.5 injects new modules (`match_telemetry`, `target_memory`, `combat_situation`, `movement_policy`, `death_classifier`) and wires them into `bot_engine.py`. If import errors or API contract breaks creep in, the whole bot breaks silently.

This plan catches regressions before any AI code runs.

---

## What to Do

### Step 1: Capture pre-change state

Run these commands and record output. Any FAIL means the codebase is already broken before 12.5 starts:

```powershell
cd "e:\Macro\GPO BR\tuitaolaso1"
python -m py_compile src/core/bot_engine.py
python -m py_compile src/services/discord_event_service.py
python -m py_compile src/zedsu_backend.py
python -m py_compile src/zedsu_core.py
python -m py_compile src/core/vision.py
python -m py_compile src/core/vision_yolo.py
```

Expected: all pass with no output.

### Step 2: Verify Phase 12.4 Discord contract

Read `src/services/discord_event_service.py` and verify:
- `emit_event()` function exists and accepts `(kind, **kwargs)`
- `capture_screenshot_png_bytes()` exists
- `should_dispatch()`, `dedupe_kill_milestone()`, `reset_kill_dedupe()` exist
- `DiscordEvent` dataclass exists
- Worker queue uses `queue.Queue`

### Step 3: Verify bot_engine.py Phase 12.4 hooks

Read `src/core/bot_engine.py` and verify:
- `_emit_death_event_once()` exists at line ~600 with `source` parameter
- `handle_post_match()` exists at line ~1544 and calls `_callbacks.emit_event("match_end", ...)`
- `CombatStateMachine.update()` has kill_milestone check with `_kill_milestone_sent` dict
- `mark_match_active()` resets `_death_event_sent = False`
- `_combat_sm.on_death()` exists and calls `_emit_death_event_once`
- `bot_loop()` has `emit_event("combat_start", ...)` in the combat start path

### Step 4: Verify config contract

Read `src/utils/config.py` and verify:
- `DEFAULT_CONFIG` has `discord_events` key with `webhook_url`, `events` dict, `kill_milestones` list
- `discord_events.events` has: `match_end`, `kill_milestone`, `combat_start`, `death`, `bot_error`
- `_deep_merge()` function exists and handles nested dicts

### Step 5: Verify service layer

Read `src/services/__init__.py` and verify all Phase 12.1–12.4 exports are present:
- `region_service`: `list_regions`, `set_region`, `delete_region`, `resolve_region`, `resolve_all_regions`
- `position_service`: `list_positions`, `set_position`, `delete_position`, `resolve_position`, `resolve_all_positions`
- `discord_event_service`: `emit_event`, `capture_screenshot_png_bytes`, `should_dispatch`, `dedupe_kill_milestone`, `reset_kill_dedupe`

### Step 6: Secret leak regression

Read `src/zedsu_backend.py` and verify `/state` handler sanitizes `discord_events.webhook_url`.

### Step 7: Combat region/position load

Verify `combat_regions_v2` and `combat_positions` keys exist in `DEFAULT_CONFIG`.

---

## Exit Criteria

All 7 steps must pass before proceeding to Wave 1. If any step fails, fix the regression first — 12.5 must start on a clean codebase.
