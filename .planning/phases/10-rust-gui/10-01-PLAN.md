---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ZedsuFrontend/index.html
  - src/ZedsuFrontend/src/lib.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "HUD displays FSM state with status color and emoji"
    - "HUD displays elapsed time, kills, and detection speed stats"
    - "State color transitions animate smoothly (CSS transitions)"
    - "JetBrains Mono font renders without pixel jitter"
  artifacts:
    - path: "src/ZedsuFrontend/index.html"
      provides: "HUD overlay with glassmorphism and neon glow"
      min_lines: 150
    - path: "src/ZedsuFrontend/src/lib.rs"
      provides: "HUD window creation + state polling commands"
      exports: ["get_hud_state"]
  key_links:
    - from: "index.html"
      to: "lib.rs"
      via: "window.__TAURI__.core.invoke('get_hud_state')"
      pattern: "invoke.*get_hud_state"
---

<objective>
Replace the placeholder index.html with a fully functional combat HUD overlay.

Purpose: Deliver the visual frontend — the 300×80px glassmorphism overlay that shows bot state to the user.

Output: Complete HUD HTML/CSS/JS with state polling, status colors, and CSS transitions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ZedsuFrontend/index.html (existing placeholder)
@src/ZedsuFrontend/src/lib.rs (BackendManager + IPC commands)
@.planning/phases/10-rust-gui/10-UI-SPEC.md (UI design contract)
@.planning/phases/10-rust-gui/10-CONTEXT.md (D-10a through D-10f decisions)

## Backend State Contract
GET /state returns:
```json
{
  "running": true,
  "status": "SCANNING LOBBY",
  "status_color": "#2563eb",
  "combat": {"combat_state": "IDLE", "kills": 0},
  "stats": {"kills": 0, "match_count": 0}
}
```

## FSM State → Emoji Mapping (from UI-SPEC)
| State | Emoji |
|-------|-------|
| IDLE | ○ |
| SCANNING | 🔵 |
| APPROACH | 🟡 |
| ENGAGED | 🔴 |
| FLEEING | 🟠 |
| SPECTATING | 🟡 |
| POST_MATCH | 🟢 |
| ERROR | ❌ |

## Status Colors (from D-10c-03, UI-SPEC)
| State | Color | Animation |
|-------|-------|-----------|
| IDLE | #ffffff | none |
| SCANNING | #3b82f6 | pulse |
| COMBAT | #ef4444 | glow |
| SPECTATING | #eab308 | none |
| POST_MATCH | #22c55e | none |
| ERROR | #ef4444 | blink |
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/ZedsuFrontend/src/lib.rs (BackendManager methods):
```rust
pub fn get_state(&self) -> Result<BackendState, String>
// Returns BackendState { running, status, status_color, logs, combat, vision, stats, config }
```

Tauri IPC command available:
```javascript
await window.__TAURI__.core.invoke('get_backend_state')
// Returns BackendState JSON
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create HUD HTML structure with glassmorphism</name>
  <files>src/ZedsuFrontend/index.html</files>
  <read_first>
    src/ZedsuFrontend/index.html (existing placeholder)
    .planning/phases/10-rust-gui/10-UI-SPEC.md (UI specs)
  </read_first>
  <action>
    Replace the entire index.html with a complete HUD overlay implementation:

    1. DOCTYPE html with lang="en"
    2. Load JetBrains Mono from Google Fonts: https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap
    3. CSS (in <style> tag):
       - Body: background transparent, font-family 'JetBrains Mono', 300x80px container
       - .hud-container: background rgba(10,10,10,0.5), backdrop-filter blur(8px), border-radius 8px, box-shadow neon glow
       - .state-row: 20px bold text, centered, displays [EMOJI STATE_NAME] format
       - .stats-row: 12px regular text, muted gray #9ca3af, displays elapsed | kills | detection ms
       - .state-text: transition color 0.3s ease, text-shadow 0.3s ease
       - @keyframes pulse: opacity 1 to 0.6 for SCANNING state
       - @keyframes blink: opacity 1 to 0 for ERROR state
       - CSS variable system for status colors
    4. HTML structure:
       - div.hud-container
         - div.state-row > span#state-text
         - div.stats-row > span#stats-text
    5. JavaScript (in <script> tag):
       - FSM state to emoji map (from UI-SPEC)
       - FSM state to color/glow map
       - State polling: setInterval every 1000ms calling get_backend_state
       - Elapsed timer tracking (local start time on first START)
       - DOM update function: set state-text content + color + animation class
       - DOM update function: set stats-text content with elapsed, kills, detection ms
       - Initial state: show [ ○ IDLE ] with white color, no animation
       - Handle errors gracefully (show ERROR state with blink)
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 | head -20</automated>
  </verify>
  <done>
    index.html contains complete HUD with glassmorphism, JetBrains Mono, 2-row layout, state polling, CSS transitions
  </done>
</task>

<task type="auto">
  <name>Task 2: Add HUD state IPC command to lib.rs</name>
  <files>src/ZedsuFrontend/src/lib.rs</files>
  <read_first>
    src/ZedsuFrontend/src/lib.rs (lines 1-55 for types, 254-292 for IPC commands)
    src/ZedsuFrontend/index.html (after Task 1 completes)
  </read_first>
  <action>
    Modify lib.rs to add HUD-specific state endpoint:

    1. Add a new IPC command `get_hud_state` that:
       - Calls backend.get_state() to get full BackendState
       - Transforms it into a HUD-specific format with:
         - combat_state: the emoji + state name string (e.g., "[ 🔴 COMBAT ]")
         - stats: elapsed time, kills, detection_ms
         - status_color: the hex color for current state
         - running: boolean from backend
       - Returns this as a JSON object the JS can directly use

    2. Add get_hud_state to the tauri::generate_handler![] macro (line 334)

    3. Keep get_backend_state for debugging/compatibility (don't remove it)

    DO NOT modify the BackendManager struct or existing HTTP methods.
    DO NOT change the process supervisor or health check thread.
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 | grep -E "(error|warning)" | head -10</automated>
  </verify>
  <done>
    get_hud_state IPC command exists and returns {combat_state, stats, status_color, running}
  </done>
</task>

</tasks>

<verification>
- cargo check passes with no errors
- index.html contains JetBrains Mono font import
- CSS includes glassmorphism (rgba background + backdrop-filter blur)
- CSS includes neon glow (box-shadow + text-shadow)
- CSS includes pulse and blink animations
- State polling setInterval configured for 1000ms
- FSM emoji map includes all 8 states (IDLE, SCANNING, APPROACH, ENGAGED, FLEEING, SPECTATING, POST_MATCH, ERROR)
- get_hud_state command added to lib.rs and registered in generate_handler
</verification>

<success_criteria>
- HUD displays "[ ○ IDLE ]" on startup
- State text color matches FSM state
- Stats row shows "⏱ 00:00 | ⚔ Kills: 0 | ⚡ 0ms" initially
- CSS transitions animate color changes smoothly (0.3s)
- SCANNING state has blue color with pulse animation
- COMBAT/ENGAGED state has red color with glow effect
- ERROR state has red color with blink animation
</success_criteria>

<output>
After completion, create `.planning/phases/10-rust-gui/10-01-SUMMARY.md`
</output>
