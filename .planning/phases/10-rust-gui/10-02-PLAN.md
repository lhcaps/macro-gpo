---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ZedsuFrontend/Cargo.toml
  - src/ZedsuFrontend/tauri.conf.json
  - src/ZedsuFrontend/capabilities/default.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Main window starts hidden (visible: false)"
    - "HUD overlay window configured (300x80px, top-right)"
    - "tauri-plugin-global-shortcut dependency added"
    - "System tray icon configured"
  artifacts:
    - path: "src/ZedsuFrontend/Cargo.toml"
      provides: "Rust dependencies including global-shortcut plugin"
      exports: ["tauri-plugin-global-shortcut"]
    - path: "src/ZedsuFrontend/tauri.conf.json"
      provides: "Window config: hidden main + HUD overlay + tray"
      exports: ["main window hidden", "hud window 300x80"]
  key_links:
    - from: "tauri.conf.json"
      to: "Cargo.toml"
      via: "global-shortcut feature flag"
      pattern: "tauri-plugin-global-shortcut"
---

<objective>
Configure Tauri for hidden main window, HUD overlay window, and hotkey system.

Purpose: Set up the window configuration and Rust dependencies for Phase 10 features.

Output: Updated Cargo.toml, tauri.conf.json, and capabilities file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ZedsuFrontend/Cargo.toml (existing dependencies)
@src/ZedsuFrontend/tauri.conf.json (existing window config)
@src/ZedsuFrontend/capabilities/default.json (existing permissions)
@.planning/phases/10-rust-gui/10-CONTEXT.md (D-10a, D-10d, D-10f decisions)
@bridger_source/src/bridger.rs (reference overlay window pattern, lines 278-346)
</context>

<interfaces>
<!-- Bridger overlay window pattern for reference -->

From bridger_source/src/bridger.rs:
```rust
// Status overlay window creation pattern
WebviewWindowBuilder::new(
    &app,
    "status_overlay",
    tauri::webview::WebviewUrl::App("overlay.html".into()),
)
.inner_size(360.0, 200.0)  // Phase 10: 300x80px instead
.resizable(false)
.decorations(false)
.transparent(true)
.always_on_top(true)
.skip_taskbar(true)
.position(100.0, 100.0)
.build()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add global-shortcut plugin to Cargo.toml</name>
  <files>src/ZedsuFrontend/Cargo.toml</files>
  <read_first>
    src/ZedsuFrontend/Cargo.toml (existing dependencies)
  </read_first>
  <action>
    Update Cargo.toml to add tauri-plugin-global-shortcut:

    1. Add to [dependencies] section:
       ```toml
       tauri-plugin-global-shortcut = "2"
       ```

    2. Add tray-icon feature to tauri (if not present):
       ```toml
       tauri = { version = "2", features = ["tray-icon"] }
       ```

    DO NOT remove any existing dependencies.
    DO NOT change tauri version.
    Keep features minimal — only add what's needed.
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 | head -5</automated>
  </verify>
  <done>
    Cargo.toml includes tauri-plugin-global-shortcut dependency
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure tauri.conf.json for hidden main + HUD overlay</name>
  <files>src/ZedsuFrontend/tauri.conf.json</files>
  <read_first>
    src/ZedsuFrontend/tauri.conf.json (existing window config)
  </read_first>
  <action>
    Update tauri.conf.json to configure:

    1. Main window (existing): Change "visible" from true to false (D-10a-01)
       - This makes the app start invisible

    2. Add HUD overlay window configuration:
       ```json
       {
         "label": "hud",
         "title": "Zedsu HUD",
         "width": 300,
         "height": 80,
         "resizable": false,
         "decorations": false,
         "transparent": true,
         "alwaysOnTop": true,
         "skipTaskbar": true,
         "visible": true,
         "url": "index.html"
       }
       ```

    3. Add system tray configuration:
       ```json
       "trayIcon": {
         "iconPath": "icons/icon.png",
         "iconAsTemplate": false
       }
       ```
       Note: If icons don't exist, create a simple 32x32 PNG placeholder at src/ZedsuFrontend/icons/icon.png

    4. Position HUD at top-right: Set x/y coordinates in pixels for 1920x1080 primary monitor
       - Right edge: 1920 - 300 - 20 = 1700 (20px margin from right)
       - Top edge: 20 (20px margin from top)

    DO NOT remove the main window entry.
    DO NOT change window labels from "main" and "hud".
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 | grep -E "(error|warning.*tauri)" | head -5</automated>
  </verify>
  <done>
    tauri.conf.json has: main window visible:false, hud window 300x80 top-right, tray icon configured
  </done>
</task>

<task type="auto">
  <name>Task 3: Update capabilities for global-shortcut and tray permissions</name>
  <files>src/ZedsuFrontend/capabilities/default.json</files>
  <read_first>
    src/ZedsuFrontend/capabilities/default.json (existing permissions)
  </read_first>
  <action>
    Update capabilities/default.json to add permissions for:

    1. Global shortcut plugin:
       ```json
       "permissions": [
         "core:default",
         "global-shortcut:allow-register",
         "global-shortcut:allow-unregister",
         "global-shortcut:allow-is-registered"
       ]
       ```

    2. Tray icon (if needed):
       ```json
       "core:tray:default"
       ```

    DO NOT add unnecessary permissions.
    DO NOT grant broad "*" permissions.
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 | head -5</automated>
  </verify>
  <done>
    default.json includes global-shortcut and tray permissions
  </done>
</task>

</tasks>

<verification>
- cargo check passes with no errors related to tauri plugins
- tauri.conf.json has main.visible set to false
- tauri.conf.json has hud window with 300x80 dimensions
- Cargo.toml includes tauri-plugin-global-shortcut
- capabilities includes global-shortcut permissions
</verification>

<success_criteria>
- App starts with no visible window
- HUD overlay window is configured (will be created by lib.rs in Plan 03)
- F1-F4 hotkey bindings will work (registered in Plan 03)
- System tray icon placeholder exists
</success_criteria>

<output>
After completion, create `.planning/phases/10-rust-gui/10-02-SUMMARY.md`
</output>
