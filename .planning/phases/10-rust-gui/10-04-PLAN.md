---
phase: 10
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo check passes without errors"
    - "cargo build produces working binary"
    - "App starts without panics or crashes"
  artifacts:
    - path: "src/ZedsuFrontend/target/debug/zedsu_frontend"
      provides: "Working Tauri binary"
      min_lines: 1
  key_links: []
---

<objective>
Verify and finalize Phase 10 implementation — cargo check, build test, and final validation.

Purpose: Ensure all components compile and the app starts correctly.

Output: Working Tauri binary with HUD overlay and hotkey system.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ZedsuFrontend/src/lib.rs (after Plan 03)
@src/ZedsuFrontend/index.html (after Plan 01)
@src/ZedsuFrontend/Cargo.toml (after Plan 02)
@src/ZedsuFrontend/tauri.conf.json (after Plan 02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cargo check and fix any errors</name>
  <files>src/ZedsuFrontend/src/lib.rs</files>
  <read_first>
    src/ZedsuFrontend/src/lib.rs
    src/ZedsuFrontend/Cargo.toml
  </read_first>
  <action>
    Run comprehensive cargo check and fix any compilation errors:

    1. Execute: cargo check --manifest-path src/ZedsuFrontend/Cargo.toml
    2. If errors found:
       - Read error messages carefully
       - Fix type mismatches, missing imports, or API changes
       - Common issues: plugin initialization order, Arc<Mutex> state management, window handle lifetimes
    3. Re-run cargo check until all errors are resolved

    DO NOT change the architecture or core logic.
    DO ONLY fix compilation errors.
  </action>
  <verify>
    <automated>cargo check --manifest-path src/ZedsuFrontend/Cargo.toml 2>&1 && echo "CHECK PASSED"</automated>
  </verify>
  <done>
    cargo check passes with no errors or warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Build release binary and verify</name>
  <files>src/ZedsuFrontend/src/lib.rs</files>
  <read_first>
    src/ZedsuFrontend/src/lib.rs
  </read_first>
  <action>
    Build the release binary to ensure full compilation works:

    1. Execute: cargo build --release --manifest-path src/ZedsuFrontend/Cargo.toml
    2. Verify the binary was created at target/release/zedsu_frontend.exe (or .exe name from Cargo.toml)
    3. Run the binary briefly to verify:
       - App starts without panic
       - HUD window appears in top-right
       - Backend process spawns

    NOTE: This is a quick smoke test. Full runtime testing requires running the app and pressing hotkeys.
  </action>
  <verify>
    <automated>if (Test-Path "src/ZedsuFrontend/target/release/zedsu_frontend.exe") { "BUILD SUCCESS" } else { "BUILD FAILED" }</automated>
  </verify>
  <done>
    Release binary exists and app starts without immediate crash
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Manual verification of HUD and hotkeys</name>
  <files>src/ZedsuFrontend/src/lib.rs</files>
  <read_first>
    src/ZedsuFrontend/index.html
    src/ZedsuFrontend/tauri.conf.json
  </read_first>
  <action>
    Run the app and manually verify Phase 10 features:

    Verification steps:
    1. Start the app (cargo run --manifest-path src/ZedsuFrontend/Cargo.toml or run the .exe)
    2. Verify main window is hidden (no large window appears)
    3. Verify HUD overlay appears in top-right corner (300x80px glassmorphism)
    4. Verify HUD shows "[ ○ IDLE ]" with white color
    5. Press F1 → should trigger emergency stop (backend should respond)
    6. Press F2 → HUD should hide
    7. Press F2 again → HUD should show
    8. Press F4 → main window should appear
    9. Close app cleanly

    Expected behaviors:
    - Main window starts invisible
    - HUD shows FSM state with status color
    - F1-F4 hotkeys work as specified
  </action>
  <verify>
    <automated>MISSING — manual verification required for hotkey and window behavior</automated>
  </verify>
  <done>
    Human confirms: HUD visible, hotkeys F1-F4 functional, main window hidden on start
  </done>
</task>

</tasks>

<verification>
- cargo check passes
- cargo build --release succeeds
- Binary exists at target/release/zedsu_frontend.exe
- HUD overlay visible at top-right on startup
- F1 triggers emergency stop
- F2 toggles HUD visibility
- F3 toggles start/stop
- F4 reveals main window
- Main window hidden on initial startup
</verification>

<success_criteria>
- cargo check: PASS (no errors)
- cargo build: PASS (binary created)
- HUD overlay: 300x80px glassmorphism visible in top-right
- Main window: starts hidden, appears on F4
- Hotkeys: F1-F4 all functional
- No runtime panics on startup
</success_criteria>

<output>
After completion, create `.planning/phases/10-rust-gui/10-04-SUMMARY.md`
</output>
