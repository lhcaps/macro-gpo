---
phase: 01-runtime-run-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/run_analysis.py
  - src/ui/app.py
autonomous: true
requirements:
  - OPER-01
  - OPER-02
  - OPER-03
  - OPER-04
  - OPER-05
user_setup: []
must_haves:
  truths:
    - The dashboard shows recent run insights without opening the raw log file.
    - Long waits, movement fallbacks, and melee-confirmation instability are summarized in plain language.
    - Recommendations point operators toward the most relevant capture/setup actions.
  artifacts:
    - src/utils/run_analysis.py
    - src/ui/app.py
  key_links:
    - refresh_runtime_summary consumes cached log-analysis output
    - dashboard labels render metrics and recommendations from the analyzer
---

<objective>
Add a cached runtime-log analyzer and surface the resulting diagnostics in the dashboard.

Purpose: Turn repeated run history into actionable guidance for the operator.
Output: A reusable analyzer utility plus a dashboard card that reports recent run health.
</objective>

<execution_context>
@E:/Macro/GPO BR/tuitaolaso1/.codex/get-shit-done/workflows/execute-plan.md
@E:/Macro/GPO BR/tuitaolaso1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-run-diagnostics/01-CONTEXT.md
@README.md
@src/ui/app.py
@src/core/bot_engine.py
@src/utils/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cached log-analysis utility for repeated run patterns</name>
  <files>src/utils/run_analysis.py</files>
  <read_first>src/core/bot_engine.py, src/utils/config.py</read_first>
  <action>Create a log-analysis utility that accepts the runtime debug log path, supports both short and full timestamp formats, caches results by file metadata, and extracts per-match timing plus repeated fallback counters that mirror the existing runtime flow.</action>
  <verify>python -c "from src.utils.run_analysis import build_runtime_log_insights; print(build_runtime_log_insights('debug_log.txt', combat_asset_ready=False)['headline'])"</verify>
  <acceptance_criteria>
    - `src/utils/run_analysis.py` parses the existing runtime messages emitted by `BotEngine`
    - analyzer output includes summary text and recommendations derived from repeated patterns
  </acceptance_criteria>
  <done>Analyzer returns stable, human-readable insights for runtime log files.</done>
</task>

<task type="auto">
  <name>Task 2: Surface run insights in the dashboard runtime summary</name>
  <files>src/ui/app.py</files>
  <read_first>src/ui/app.py, src/utils/run_analysis.py</read_first>
  <action>Add a dedicated dashboard card for recent run insights, refresh it from the existing summary loop, and show a compact headline, metrics summary, and operator recommendations without breaking current setup/readiness behavior.</action>
  <verify>python -m compileall src main.py</verify>
  <acceptance_criteria>
    - dashboard code imports and uses the analyzer utility
    - labels clearly surface summary plus recommended next actions
  </acceptance_criteria>
  <done>Operators can understand repeated run behavior from the dashboard without opening the raw log.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall src main.py`
- [ ] `python -c "from src.utils.run_analysis import build_runtime_log_insights; print(build_runtime_log_insights('debug_log.txt', combat_asset_ready=False))"`
- [ ] Analyzer can read the provided multi-run log file and emit meaningful output
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dashboard insight text stays consistent with the current control-center tone
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-run-diagnostics/01-01-SUMMARY.md`
</output>
