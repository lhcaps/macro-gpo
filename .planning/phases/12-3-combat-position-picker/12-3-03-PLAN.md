---
phase: "12-3-combat-position-picker"
plan: "03"
type: execute
wave: "2"
depends_on: ["12-3-01-PLAN.md", "12-3-02-PLAN.md"]
files_modified: []
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "Click inside window only (outside returns clear error, NOT silently clamped)"
    - "Position survives window resize (normalized coords)"
    - "emergency_stop cancels overlay safely (via _active_overlay tracking)"
  artifacts:
    - path: "src/overlays/position_picker.py"
      provides: "PositionPickerOverlay module with out-of-bounds rejection"
    - path: "src/overlays/__init__.py"
      provides: "PositionPickerOverlay export"
    - path: "src/zedsu_backend.py"
      provides: "pick_position handler + _active_overlay + emergency_stop wired"
---

<objective>
Verify Phase 12.3 implementation: module imports, syntax, contract patterns, and the three exit criteria (out-of-bounds rejection, normalized coords, emergency_stop overlay cancel).
</objective>

<context>
@src/overlays/position_picker.py      — after Plan 01 executes
@src/overlays/__init__.py             — after Plan 01 executes
@src/zedsu_backend.py                 — after Plan 02 executes
</context>

<tasks>

<task type="verify">
  <name>Task 1: Import smoke — position_picker module</name>
  <files></files>
  <action>
Run: `python -c "from src.overlays.position_picker import PositionPickerOverlay; print('OK')"`
Expected: prints "OK", exits 0
</action>
  <verify>
    <automated>python -c "from src.overlays.position_picker import PositionPickerOverlay; print('OK')"</automated>
  </verify>
  <done>PositionPickerOverlay imports cleanly from src.overlays.position_picker</done>
</task>

<task type="verify">
  <name>Task 2: Import smoke — overlays __init__</name>
  <files></files>
  <action>
Run: `python -c "from src.overlays import PositionPickerOverlay, RegionSelectorOverlay; print('OK')"`
Expected: prints "OK", exits 0
</action>
  <verify>
    <automated>python -c "from src.overlays import PositionPickerOverlay, RegionSelectorOverlay; print('OK')"</automated>
  </verify>
  <done>Both overlays exported from src.overlays package</done>
</task>

<task type="verify">
  <name>Task 3: Backend syntax check</name>
  <files></files>
  <action>
Run: `python -m py_compile src/zedsu_backend.py`
Expected: exits 0 (no errors)
</action>
  <verify>
    <automated>python -m py_compile src/zedsu_backend.py</automated>
  </verify>
  <done>zedsu_backend.py compiles without syntax errors</done>
</task>

<task type="verify">
  <name>Task 4: pick_position in do_POST handler</name>
  <files></files>
  <action>
Run: `python -c "import ast; code=open('src/zedsu_backend.py').read(); tree=ast.parse(code); [print(n.name) for n in ast.walk(tree) if isinstance(n,ast.FunctionDef) and n.name=='do_POST']; actions=[n for n in ast.walk(tree) if isinstance(n,ast.Compare) and hasattr(n.left,'s') and n.left.s=='action']; print('action vars found:', len(actions))"`
Expected: do_POST found, action comparison found
</action>
  <verify>
    <automated>python -c "import ast;code=open('src/zedsu_backend.py',encoding='utf-8').read();tree=ast.parse(code);funcs=[n.name for n in ast.walk(tree) if isinstance(n,ast.FunctionDef)];assert'do_POST'in funcs;assert'elif action == \"pick_position\":'in code;print('OK')"</automated>
  </verify>
  <done>pick_position action exists in do_POST handler</done>
</task>

<task type="verify">
  <name>Task 5: _active_overlay in emergency_stop handler</name>
  <files></files>
  <action>
Run a Python script that reads the backend file and verifies `_active_overlay` appears at least 3 times (module-level declaration, set in pick_position, checked in emergency_stop).
```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()
lines = [l.strip() for l in s.splitlines()]
matches = [(i+1, l) for i, l in enumerate(lines) if '_active_overlay' in l]
print(f'_active_overlay occurrences: {len(matches)}')
for num, line in matches:
    print(f'  line {num}: {line}')
assert len(matches) >= 3, f'Expected >=3, got {len(matches)}'
print('OK')
"
```
Expected: at least 3 lines with `_active_overlay`
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();m=[l.strip() for l in s.splitlines() if '_active_overlay' in l];print(f'_active_overlay occurrences: {len(m)}');[print(f'  line {i+1}: {l}') for i,l in enumerate(m)];assert len(m)>=3;print('OK')"</automated>
  </verify>
  <done>_active_overlay used in at least 3 places: global declaration, set in pick_position, checked in emergency_stop</done>
</task>

<task type="verify">
  <name>Task 6: set_position called in pick_position handler</name>
  <files></files>
  <action>
Run a Python script that verifies `set_position` is called in the pick_position block with `captured_at` and `window_title`. Uses proper block extraction instead of fixed char slices.

The pick_position block extends from `elif action == "pick_position":` to the next `elif` or `else` at the same indentation level. The `set_position()` call appears well past 1000 chars into the block, so a fixed slice would miss it.

```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()
# Find pick_position block boundaries
idx = s.find('elif action == \"pick_position\":')
assert idx >= 0, 'pick_position block not found'
# Find next elif action ==  at same indentation level (16 spaces)
search_from = idx + len('elif action == \"pick_position\":')
next_elif = s.find('\n            elif action ==', search_from)
next_else = s.find('\n            else:', search_from)
end = min(x for x in [next_elif, next_else] if x >= 0)
block = s[idx:end]
# set_position and its args must be inside the block
assert 'set_position(' in block, 'set_position not found in pick_position block'
assert 'captured_at' in block, 'captured_at not passed to set_position'
assert 'window_title' in block, 'window_title not passed to set_position'
# Verify set_position comes after the try/finally structure
try_start = block.find('try:')
set_pos = block.find('set_position(')
assert try_start >= 0 and set_pos > try_start, 'set_position not inside try block'
print('OK')
"
```
Expected: all assertions pass
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();idx=s.find('elif action == \"pick_position\":');assert idx>=0;sf=idx+30;ne=s.find('\n            elif action ==',sf);ns=s.find('\n            else:',sf);end=min(x for x in [ne,ns] if x>=0);b=s[idx:end];assert'set_position('in b;assert'captured_at'in b;assert'window_title'in b;print('OK')"</automated>
  </verify>
  <done>set_position called with captured_at and window_title in pick_position handler</done>
</task>

<task type="verify">
  <name>Task 7: save_config after set_position</name>
  <files></files>
  <action>
Run a Python script that verifies `save_config` is called after `set_position` in the pick_position block. Uses 3000-char window which covers the entire pick_position block from action name to end.

```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()
idx = s.find('elif action == \"pick_position\":')
block = s[idx:idx+3000]
si = block.find('set_position(')
assert si >= 0, 'set_position not found'
# save_config must appear after set_position in this block
sc = block.find('save_config(', si)
assert sc >= 0, 'save_config not found after set_position'
print('OK')
"
```
Expected: save_config found after set_position in pick_position block
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();idx=s.find('elif action == \"pick_position\":');block=s[idx:idx+3000];si=block.find('set_position(');sc=block.find('save_config(',si);assert si>=0;assert sc>=0;print('OK')"</automated>
  </verify>
  <done>save_config() called after set_position in pick_position handler</done>
</task>

<task type="verify">
  <name>Task 8: request_cancel in emergency_stop</name>
  <files></files>
  <action>
Run a Python script that verifies `request_cancel` is called in both pick_position and emergency_stop blocks. Uses proper block extraction instead of fixed char slices.

The emergency_stop block also needs proper boundaries — the 200-char slice is too short and may miss `request_cancel` if there are comments between the action name and the call.

```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()

# Verify request_cancel exists globally
assert 'request_cancel' in s, 'request_cancel not found in backend'

# Extract pick_position block (already verified in Task 6)
idx_pp = s.find('elif action == \"pick_position\":')
assert idx_pp >= 0, 'pick_position not found'
search_pp = idx_pp + 30
ne_pp = s.find('\n            elif action ==', search_pp)
ns_pp = s.find('\n            else:', search_pp)
end_pp = min(x for x in [ne_pp, ns_pp] if x >= 0)
block_pp = s[idx_pp:end_pp]
assert 'request_cancel' in block_pp, 'request_cancel not in pick_position block'

# Extract emergency_stop block using proper boundaries
idx_es = s.find('elif action == \"emergency_stop\":')
assert idx_es >= 0, 'emergency_stop block not found'
search_es = idx_es + 30
ne_es = s.find('\n            elif action ==', search_es)
ns_es = s.find('\n            else:', search_es)
end_es = min(x for x in [ne_es, ns_es] if x >= 0)
block_es = s[idx_es:end_es]
assert '_active_overlay' in block_es, '_active_overlay not in emergency_stop block'
assert 'request_cancel' in block_es, 'request_cancel not in emergency_stop block'

print('OK')
"
```
Expected: all assertions pass
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();assert'request_cancel'in s;idx_pp=s.find('elif action == \"pick_position\":');assert idx_pp>=0;sp=idx_pp+30;ne_p=s.find('\n            elif action ==',sp);ns_p=s.find('\n            else:',sp);ep=min(x for x in [ne_p,ns_p] if x>=0);bp=s[idx_pp:ep];assert'request_cancel'in bp;idx_es=s.find('elif action == \"emergency_stop\":');assert idx_es>=0;se=idx_es+30;ne_e=s.find('\n            elif action ==',se);ns_e=s.find('\n            else:',se);ee=min(x for x in [ne_e,ns_e] if x>=0);be=s[idx_es:ee];assert'_active_overlay'in be;assert'request_cancel'in be;print('OK')"</automated>
  </verify>
  <done>request_cancel() called on _active_overlay in emergency_stop</done>
</task>

<task type="verify">
  <name>Task 9: Timeout value 300.0 in pick_position</name>
  <files></files>
  <action>
Run a Python script that verifies `get_result(timeout=300` appears in the pick_position block.
```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()
idx = s.find('elif action == \"pick_position\":')
block = s[idx:idx+3000]
assert 'get_result(timeout=300' in block, 'get_result(timeout=300) not in pick_position block'
print('OK')
"
```
Expected: timeout=300 found in pick_position
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();idx=s.find('elif action == \"pick_position\":');block=s[idx:idx+3000];assert'get_result(timeout=300'in block;print('OK')"</automated>
  </verify>
  <done>5-minute timeout (300.0s) set in pick_position get_result call</done>
</task>

<task type="verify">
  <name>Task 10: PositionPickerOverlay instantiated in pick_position</name>
  <files></files>
  <action>
Run a Python script that verifies `PositionPickerOverlay(` is instantiated in the pick_position block.
```
python -c "
s = open('src/zedsu_backend.py', encoding='utf-8').read()
idx = s.find('elif action == \"pick_position\":')
block = s[idx:idx+3000]
assert 'PositionPickerOverlay(' in block, 'PositionPickerOverlay not instantiated in pick_position block'
print('OK')
"
```
Expected: PositionPickerOverlay instantiated in pick_position
</action>
  <verify>
    <automated>python -c "s=open('src/zedsu_backend.py',encoding='utf-8').read();idx=s.find('elif action == \"pick_position\":');block=s[idx:idx+3000];assert'PositionPickerOverlay('in block;print('OK')"</automated>
  </verify>
  <done>PositionPickerOverlay instantiated in pick_position handler</done>
</task>

<task type="verify">
  <name>Task 11: Out-of-bounds error message in position_picker.py</name>
  <files></files>
  <action>
Run a Python script that verifies the out-of-bounds error message exists in position_picker.py.
```
python -c "
s = open('src/overlays/position_picker.py', encoding='utf-8').read()
assert 'Click outside game window' in s, 'Out-of-bounds error message not found'
print('OK')
"
```
Expected: exact message per D-02 and D-06
</action>
  <verify>
    <automated>python -c "s=open('src/overlays/position_picker.py',encoding='utf-8').read();assert'Click outside game window'in s;print('OK')"</automated>
  </verify>
  <done>Out-of-bounds rejection message present in position_picker.py</done>
</task>

<task type="verify">
  <name>Task 12: Normalized coords in _on_click</name>
  <files></files>
  <action>
Run a Python script that verifies the normalized coord computation exists in position_picker.py.
```
python -c "
s = open('src/overlays/position_picker.py', encoding='utf-8').read()
assert 'event.x / self._win_width' in s or 'event.x/self._win_width' in s, 'norm_x computation not found'
assert 'event.y / self._win_height' in s or 'event.y/self._win_height' in s, 'norm_y computation not found'
print('OK')
"
```
Expected: `event.x / self._win_width` and `event.y / self._win_height` found
</action>
  <verify>
    <automated>python -c "s=open('src/overlays/position_picker.py',encoding='utf-8').read();assert'event.x / self._win_width'in s or'event.x/self._win_width'in s;assert'event.y / self._win_height'in s or'event.y/self._win_height'in s;print('OK')"</automated>
  </verify>
  <done>Normalized [0,1] coords computed via division by window dimensions</done>
</task>

</tasks>

<success_criteria>
All 12 verification checks pass:
- Tasks 1-2: Both overlay modules import cleanly
- Tasks 3-4: Backend compiles, pick_position action present
- Tasks 5-10: _active_overlay tracking, set_position with metadata, save_config, request_cancel, timeout, instantiation all verified
- Tasks 11-12: Exit criteria verified — out-of-bounds error message present, normalized coords computed
</success_criteria>

<output>
After completion, create `.planning/phases/12-3-combat-position-picker/12-3-03-SUMMARY.md`
</output>
